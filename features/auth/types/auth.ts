export type LoginFormValues = {
  username: string;
  password: string;
};

// Add this export if not present
export type SignupFormValues = {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  password: string;
};